# CSE389TermProject

git workflow:

git clone https://github.com/username/academichub.git
cd academichub

git pull origin main

# ... make your changes ...

git add .
git commit -m "Added calendar feature"
git push origin main
